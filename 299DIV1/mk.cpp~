#include <bits/stdc++.h>
using namespace std;

#define LL long long

int main(){
	srand(time(0));
	freopen("C.in", "w", stdout);
	int N = rand () % 10 + 1;
	printf("%d\n", N);
	for (int i = 1; i <= N; i++)
		printf("%d %d\n" , rand () % 10 + 1, rand () % 10 + 1);
}
